import { View } from 'native-base';
import styled from 'styled-components/native';

export const CartDetailsSection = styled(View)`
  padding: 10px 10px;
  border-top-color: #ccc;
  border-top-width: 1px;
`;

export const CartDetailsSectionRow = styled(View)`
  width: 100%;
  flex-direction: row;
  height: 30px;
  justify-content: space-between;
`;
